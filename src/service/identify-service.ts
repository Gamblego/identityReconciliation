import { APIRequest } from "../interface/common";

export const identifyContact = async (req: APIRequest) => {
 try {
    
 } catch (error) {
    throw new Error(`[identifyContact] Error: ${error}`)
 }
}
